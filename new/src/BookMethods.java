public interface BookMethods {
    /*you need to create concrete methods that implements those methods and make them all public or an error will be dropped*/
    void closeBook(Book book);
    void openBook(Book book);
    void addBook(Book book);
    void showBooks();

}
